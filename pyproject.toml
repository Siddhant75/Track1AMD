[project]
name = "amd-track1-agent"
version = "0.1.0"
description = "Zero-Token All-Local Gemma Agent for AMD Hackathon Track 1"
requires-python = ">=3.11"
dependencies = [
    "llama-cpp-python>=0.3.0",
    "pydantic>=2.0",
    "aiohttp>=3.9.0",
]
